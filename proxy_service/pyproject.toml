[project]
name = "proxy-service"
version = "0.1.0"
description = "Enterprise LLM Proxy with Guardrails"
dependencies = ["fastapi>=0.100.0", "uvicorn[standard]>=0.22.0"]

[tool.uv]
dev-dependencies = ["httpx", "pytest"]
