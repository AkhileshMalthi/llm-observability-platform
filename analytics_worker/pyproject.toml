[project]
name = "analytics-worker"
version = "0.1.0"
description = "Analytics Worker for processing LLM logs"
requires-python = ">=3.12"
dependencies = [
    "redis>=5.0.0",
    "clickhouse-connect>=0.7.0",
    "tiktoken>=0.6.0",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
]
